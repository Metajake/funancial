import { Meteor } from 'meteor/meteor';

import "../imports/api/amounts.js"; //IMPORTING the COLLECTIONS to the SERVER (so that db data persists between the server and client)

Meteor.startup(() => {
  // code to run on server at startup
});
