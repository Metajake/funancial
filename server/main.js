import { Meteor } from 'meteor/meteor';

import "../imports/api/amounts.js"; //IMPORTING the COLLECTION to the SERVER (why, i'm not sure)

Meteor.startup(() => {
  // code to run on server at startup
});
